# docker-compose.yml
version: "3.8"

services:
  mcp-server:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./mcp-config.toml:/app/config.toml:ro
    environment:
      - RUST_LOG=info
